import { CanvasZoomDirective } from './canvas-zoom.directive';

describe('CanvasZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasZoomDirective();
    expect(directive).toBeTruthy();
  });
});
