import { CanvasPanDirective } from './canvas-pan.directive';

describe('CanvasPanDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasPanDirective();
    expect(directive).toBeTruthy();
  });
});
