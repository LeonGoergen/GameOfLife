import { TransformationMatrix } from './transformation-matrix.model';

describe('TransformationMatrix', () => {
  it('should create an instance', () => {
    expect(new TransformationMatrix()).toBeTruthy();
  });
});
